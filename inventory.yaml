all:
  children:
    primary:
      hosts:
        mongo1:
          ansible_host:
          ansible_user:
          ansible_ssh_private_key_file:

    secondary:
      hosts:
        mongo2:
          ansible_host:
          ansible_user:
          ansible_ssh_private_key_file:
